# This file is a template, and might need editing before it works on your project.
# This is a sample GitLab CI/CD configuration file that should run without any modifications.
# It demonstrates a basic 3 stage CI/CD pipeline. Instead of real tests or scripts,
# it uses echo commands to simulate the pipeline execution.
#
# A pipeline is composed of independent jobs that run scripts, grouped into stages.
# Stages run in sequential order, but jobs within stages run in parallel.
#
# For more information, see: https://docs.gitlab.com/ee/ci/yaml/index.html#stages
#
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
#
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Getting-Started.gitlab-ci.yml

stages:          # List of stages for jobs, and their order of execution
  - build
  

build-job:       # This job runs in the build stage, which runs first.
  stage: build
  only:
      - dispute-letter
  before_script:
    - apt update
    - mkdir ~/.ssh
    - eval $(ssh-agent -s)
    - echo "$PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - chmod 700 /root/.ssh
    - ssh-keyscan -H "$PUBLIC_KEY" >> ~/.ssh/known_hosts
  script:
    - ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY rm -rf testpipeline-disputeletter 
    - ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY mkdir testpipeline-disputeletter 
    - ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd testpipeline-disputeletter && git clone https://gitlab+deploy-token-3828302:gldt-nVrsh6mn57yWZzJ7np4d@gitlab.com/Codistan/credit-butterfly/python.git"
    - ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd testpipeline-disputeletter/python && git checkout dispute-letter"
    - ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd testpipeline-disputeletter/python && python3 -m venv my-test-env"
    - ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd testpipeline-disputeletter/python && source my-test-env/bin/activate && pm2 delete dispute-test && pm2 start 'uvicorn dispute_test:app --reload --port 9001' --name=dispute-test" 
    #- ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd pipeline && source py-env/bin/activate && pm2 restart 5"
    #- ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd pipeline && git clone https://gitlab+deploy-token-3436209:oM6zSydM_cdyQo49nWS6@gitlab.com/Codistan/celayix/celayix-python.git"
    #- ssh -o StrictHostKeyChecking=no ubuntu@$PUBLIC_KEY "cd python-api/celayix-python && source py-env/bin/activate"
    


